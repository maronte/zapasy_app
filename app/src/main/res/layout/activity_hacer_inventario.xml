<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EscanearActivity">

    <FrameLayout
        android:layout_width="300dp"
        android:layout_height="350dp"
        android:id="@+id/containerFragmentScanner"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="50dp"
        app:layout_constraintTop_toTopOf="parent"
        >
        <!--fragment
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            /-->
    </FrameLayout>

    <Button
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/containerFragmentScanner"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:id="@+id/btnterminarinventario"
        android:background="@drawable/backgroud_button_rounded"
        android:layout_marginTop="50dp"
        android:textColor="@color/primaryTextColor"
        android:text="Terminar"
        android:fontFamily="sans-serif"
        />

</androidx.constraintlayout.widget.ConstraintLayout>